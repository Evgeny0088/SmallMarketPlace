spring.application.name=test

# wire mock ports
brandWireMockServer=8083
itemWireMockServer=8084

# kafka cuncumer topic
itemDTO.topic.name_1=update_all_packages

## postgres test container
#spring.datasource.url=${item-storageDB.jdbcUrl}
#spring.datasource.username=${item-storageDB.username}
#spring.datasource.password=${item-storageDB.password}

#liquidebase setting
spring.liquibase.enabled=true
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.properties.hibernate.default_schema=public
spring.test.database.replace=none
spring.liquibase.contexts=text
spring.liquibase.change-log=classpath:db/changelog/db.changelog-master-test.yaml
logging.level.liquibase=INFO