server:
  port: 9000

spring:
  application:
    name: config-Server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Evgeny0088/marketplaceConfigServer
          clone-on-start: true

